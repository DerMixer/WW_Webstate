export type PartnerType = {
    name: string
    description: string
    url: string,
    imgPath: string
}

export type widgetInformationType = {
    icon: string
    headline: string
    text: string,
    refText: string,
    url: string
}